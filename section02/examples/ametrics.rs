use std::{thread, time::Duration};
use anyhow::Result;
use rand::Rng;
use section02::AmapMetrics;

const N: usize = 2;
const M: usize = 4;

// TODO 补充单测
// cargo run --example ametrics
// cargo watch -x "run --example ametrics"

fn main() -> () {
    println!("ametrics.rs!")
}